tools:
  - class_name: "tools.cuda_tool.CudaTool"
    config: {}
    tool_schema:
      type: "function"
      function:
        name: "calc_cuda_reward"
        description: "A tool for calculating the reward of of a generated CUDA kernel. Scaled between 0.0 and 1.0"
        parameters:
          type: "object"
          properties:
            cuda_src:
              type: "string"
              description: "The source code to be evaluated. Must be a valid CUDA kernel wrapped in a PyTorch harness."
          required: ["cuda_src"]